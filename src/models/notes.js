export function Notes(userId, id, title, description, completed) {
  this.userId = userId;
  this.id = id;
  this.title = title;
  this.description = description;
  this.completed = completed;
}
